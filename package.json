{
  "name": "wazuh-faqs",
  "version": "1.0.0",
  "private": true,
  "description": "Wazuh FAQs",
  "author": "Desvelao",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "develop": "gatsby develop",
    "start": "gatsby develop",
    "build": "gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "prettier": "npx prettier --write .",
    "develop-docker": "docker run -itd --rm --name wazuh-faqs -w \"/home/node/app\" -v \"$(pwd):/home/node/app\" -p \"8000:8000\" node:14.15-alpine3.13 sh -c \"npm install -g gatsby-cli@3.9.0 && gatsby develop -H \\\"0.0.0.0\\\"\""
  },
  "dependencies": {
    "gatsby": "^3.8.1",
    "gatsby-source-filesystem": "^3.8.0",
    "gatsby-transformer-json": "^4.6.0",
    "gatsby-transformer-remark": "^4.5.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "prettier": "2.5.1"
  }
}
